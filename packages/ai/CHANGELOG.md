## 0.0.1 - Unreleased

- Establish `pigcode_ai` as the core SDK package.
- Re-export typed provider metadata and headers used by the public core API.
- Verify cross-package core interoperability and portable public-barrel
  behavior for the Phase 1 compatibility target.
