# pigcode_ai

High-level pure-Dart generation, streaming, tool-loop, middleware, structured-output, and telemetry APIs.

```dart
import 'package:pigcode_ai/pigcode_ai.dart';
```

This package is not yet published (`publish_to: none`).
