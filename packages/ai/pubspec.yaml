name: pigcode_ai
description: Pure Dart AI SDK core for generation, streaming, embeddings, reranking, media, file and skill uploads, tools, middleware, and telemetry.
version: 0.0.1
publish_to: none
repository: https://github.com/pigcode/ai
issue_tracker: https://github.com/pigcode/ai/issues

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  pigcode_ai_provider: ^0.0.1
  pigcode_ai_provider_utils: ^0.0.1
  equatable: ^2.0.0
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
