/// User-defined context that flows through a generation run.
typedef RuntimeContext = Map<String, Object?>;

/// Per-tool context map keyed by tool name.
typedef ToolsContext = Map<String, Object?>;
