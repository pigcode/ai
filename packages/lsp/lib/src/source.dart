import 'generated/lsp_inventory.g.dart';

/// LSP specification version declared by the pinned meta-model.
const lspSpecificationVersion = lspGeneratedSpecificationVersion;

/// Auditable upstream release label for the vendored snapshot.
const lspSourceRelease = lspGeneratedSourceRelease;

/// Exact upstream Git revision for the vendored snapshot.
const lspSourceRevision = lspGeneratedSourceRevision;

/// SHA-256 of the vendored LSP meta-model.
const lspMetaModelSha256 = lspGeneratedMetaModelSha256;

/// JSON Schema dialect used to validate the vendored meta-model.
const lspSchemaDialect = lspGeneratedSchemaDialect;
