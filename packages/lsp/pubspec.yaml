name: pigcode_ai_lsp
description: Portable Language Server Protocol models, codecs, and client primitives for Pigcode.
version: 0.0.1
publish_to: none
repository: https://github.com/pigcode/ai
issue_tracker: https://github.com/pigcode/ai/issues

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  pigcode_ai_protocol_utils: ^0.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
