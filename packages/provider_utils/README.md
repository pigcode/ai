# pigcode_ai_provider_utils

`pigcode_ai_provider_utils` provides shared provider utilities for HTTP, SSE,
JSON validation, multipart requests, response handlers, identifiers, media, and
streaming tool calls.

```dart
import 'package:pigcode_ai_provider_utils/pigcode_ai_provider_utils.dart';
```

This package is not yet published (publish_to: none).
