# pigcode_ai_anthropic

`pigcode_ai_anthropic` implements Anthropic Messages with tools, file uploads,
skill creation, cache control, and an explicit container ID continuation helper.

```dart
import 'package:pigcode_ai_anthropic/pigcode_ai_anthropic.dart';
```

This package is not yet published (publish_to: none).
