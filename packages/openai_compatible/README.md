# pigcode_ai_openai_compatible

`pigcode_ai_openai_compatible` provides a configurable provider foundation for
OpenAI-compatible Chat Completions and embeddings wire formats.

```dart
import 'package:pigcode_ai_openai_compatible/pigcode_ai_openai_compatible.dart';
```

This package is not yet published (publish_to: none).
