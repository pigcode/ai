## 0.0.1 - Unreleased

- Establish `pigcode_ai_openai_compatible` as the configurable OpenAI-compatible provider foundation.
- Verify configurable adapter interoperability with the shared contracts and
  its portable public barrel for the Phase 1 compatibility target.
