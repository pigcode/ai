name: pigcode_ai_openai
description: OpenAI provider for Pigcode AI with Chat Completions, Responses, embeddings, images, audio, file and skill uploads, and realtime transcription.
version: 0.0.1
publish_to: none
repository: https://github.com/pigcode/ai
issue_tracker: https://github.com/pigcode/ai/issues

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  pigcode_ai_provider: ^0.0.1
  pigcode_ai_provider_utils: ^0.0.1
  equatable: ^2.0.0
  http: ^1.6.0
  http_parser: ^4.1.2
  web_socket_channel: ^3.0.3

dev_dependencies:
  test: ^1.25.0
