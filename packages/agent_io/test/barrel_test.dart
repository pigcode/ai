// Importing the intentionally empty barrel verifies that it remains valid.
// ignore: unused_import
import 'package:pigcode_ai_agent_io/pigcode_ai_agent_io.dart';
import 'package:test/test.dart';

void main() {
  test('agent IO barrel compiles on the VM', () {
    expect(true, isTrue);
  });
}
