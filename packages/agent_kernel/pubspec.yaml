name: pigcode_ai_agent_kernel
description: Portable agent domain, command, lifecycle, and durable-store contracts for Pigcode.
version: 0.0.1
publish_to: none
repository: https://github.com/pigcode/ai
issue_tracker: https://github.com/pigcode/ai/issues

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  crypto: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
