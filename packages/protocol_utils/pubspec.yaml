name: pigcode_ai_protocol_utils
description: Portable JSON-RPC, framing, request correlation, and cancellation primitives for Pigcode protocol packages.
version: 0.0.1
publish_to: none
repository: https://github.com/pigcode/ai
issue_tracker: https://github.com/pigcode/ai/issues

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  equatable: ^2.0.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
