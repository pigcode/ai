## 0.0.1 - Unreleased

- Establish `pigcode_ai_provider` as the provider-neutral contract package.
- Verify cross-package type identity, metadata, cancellation, error, and
  portable public-barrel behavior for the Phase 1 compatibility target.
