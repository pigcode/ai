# Fixed TypeScript ACP peer

This fixture uses the official `@agentclientprotocol/sdk` package at exact
version `1.3.0` (repository tag commit
`fa32570936b42bf4f3aef175c6e926860914b638`). `package-lock.json` is committed;
`node_modules` is generated only by the peer-matrix runner and is never
committed.
